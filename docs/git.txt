# COMANDOS GIT

$ git checkout devel
$ git status
$ git pull origin devel
$ git git log
$ git add file1 file2 etc
$ git commit -m 'título do commit'
$ git status
$ git log
ou
$ git log --oneline
$ git push origin devel

Para sair do log usar 'q'
